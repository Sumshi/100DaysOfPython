#!/bin/bash

foreach($numbers as $number)
{
    echo $number;
}

#  $numbers array and prints the value of each element. It does so using a foreach loop, which simplifies the process of iterating over array elements.
